import React from 'react';

const RibbonContainer = props => (
  <div style={{ background: '#ECECEC', padding: '10px', marginTop: '5px', marginBottom: '25px', height: 'auto' }}>{props.children}</div>
);

export default RibbonContainer;
